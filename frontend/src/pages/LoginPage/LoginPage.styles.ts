import { Box, styled } from '@mui/material';

export const StyledBoxMain = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  gap: 30,
  marginTop: 25,
  alignItems: 'center',
  width: '100%', 
}));


